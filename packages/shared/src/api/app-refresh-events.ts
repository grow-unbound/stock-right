/** Dispatched on window after money mutates (web). */
export const MONEY_REFRESH_EVENT = "sr-money-refresh";

/** Dispatched on window after parties mutate (web). */
export const PARTIES_REFRESH_EVENT = "sr-parties-refresh";

/** Dispatched on window after stock / lots mutate (web). */
export const STOCK_REFRESH_EVENT = "sr-stock-refresh";

/** Dispatched on window after tenant users mutate (web). */
export const TENANT_USERS_REFRESH_EVENT = "sr-tenant-users-refresh";
