# StockRight Logo

The brand doc does not include a logo lock-up. We use a simple wordmark — "StockRight" set in **Noto Serif 600** — as a placeholder so the UI kit has a canonical brand mark to display.

See `wordmark.svg` for the SVG version. **Replace with the real lock-up when available.**
